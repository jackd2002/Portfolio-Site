{
  "name": "ze-claw",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/UIBar.ts --bundle --outfile=dist/uibar.js --format=iife --global-name=UIBarLib --platform=browser",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "gsap": "^3.12.5",
    "pixi.js": "^8.4.0"
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "typescript": "^5.4.5"
  }
}
